import { RoadmapBoard } from "@/components/admin/swarm/roadmap/RoadmapBoard";

export const metadata = { title: "Roadmap | Swarm Control" };

export default function RoadmapPage() {
  return <RoadmapBoard />;
}
