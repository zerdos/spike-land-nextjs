/**
 * Interactive Poll Template Metadata
 */

import type { TemplateMetadata } from "../types";

export const interactivePollMetadata: TemplateMetadata = {
  id: "interactive-poll",
  name: "Interactive Poll",
  description:
    "Real-time voting poll with percentage bars and live results. Prevents double voting and displays winning option.",
  purpose: "poll",
  tags: ["poll", "voting", "survey", "interactive", "engagement"],
};
