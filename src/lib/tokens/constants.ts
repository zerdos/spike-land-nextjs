/**
 * Token system constants shared across modules
 */

/** Regeneration interval in milliseconds (15 minutes) */
export const TOKEN_REGENERATION_INTERVAL_MS = 15 * 60 * 1000;

/** Number of tokens regenerated per interval */
export const TOKENS_PER_REGENERATION = 1;
