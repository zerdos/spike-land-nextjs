{
  "mcpServers": {
    "spike-land-dev": {
      "type": "url",
      "url": "http://localhost:3000/api/mcp"
    },
    "vitest": {
      "type": "stdio",
      "command": "npx",
      "args": ["vitest-mcp"]
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest"
      ]
    },
    "sentry-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@sentry/mcp-server"],
      "env": {
        "SENTRY_ACCESS_TOKEN": "${SENTRY_MCP_AUTH_TOKEN}"
      }
    }
  }
}
