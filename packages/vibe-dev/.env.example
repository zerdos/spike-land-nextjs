# vibe-dev Environment Configuration
# Copy to .env and fill in values

# ==============================================
# Required for poll command (agent processing)
# ==============================================

# Upstash Redis credentials
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxx

# Agent API key for spike.land authentication
# Same as AGENT_API_KEY in spike-land-nextjs/.env.local
AGENT_API_KEY=xxx

# Claude Code OAuth token
# Copy from spike-land-nextjs/.env.local (CLAUDE_CODE_OAUTH_TOKEN)
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxx

# spike.land API key for MCP tools
# Get one from your spike.land account settings
SPIKE_LAND_API_KEY=your-spike-land-key

# ==============================================
# Optional configuration
# ==============================================

# API base URL (defaults to https://spike.land)
# SPIKE_LAND_API_URL=https://spike.land

# Default codespace to watch (for dev mode)
CODESPACE_ID=my-app

# Custom testing.spike.land URL (for self-hosted)
# TESTING_SPIKE_LAND_URL=https://testing.spike.land
